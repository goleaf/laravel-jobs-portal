// apply_job Component
document.addEventListener('DOMContentLoaded', function() {
    try {
        // apply_job Component
document.addEventListener('DOMContentLoaded', function() {
    try {
        // apply_job Component JavaScript
// Enhanced with Universal patterns

--}}
{{-- let applyJobUrl ="{{ route('apply-job') }}"; --}}
{{-- let jobDetailsUrl ="{{ url('job-details') }}"; --}}
{{--


    } catch (error) {
        console.error('Error in apply_job component:', error);
    }
});
// apply_job Component JavaScript
// Enhanced with Universal patterns

--}}
{{-- let applyJobUrl ="{{ route('apply-job') }}"; --}}
{{-- let jobDetailsUrl ="{{ url('job-details') }}"; --}}
{{--


    } catch (error) {
        console.error('Error in apply_job component:', error);
    }
});