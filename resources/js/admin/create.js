// create Component
document.addEventListener('DOMContentLoaded', function() {
    try {
        // create Component JavaScript
// Enhanced with Context7 patterns

let isEdit = false;
        var phoneNo ="{{ old('region_code').old('phone') }}";


    } catch (error) {
        console.error('Error in create component:', error);
    }
});