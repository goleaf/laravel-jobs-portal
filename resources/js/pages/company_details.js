// company_details Component
document.addEventListener('DOMContentLoaded', function() {
    try {
        // company_details Component
document.addEventListener('DOMContentLoaded', function() {
    try {
        // company_details Component JavaScript
// Enhanced with Context7 patterns

--}}
{{-- let addCompanyFavouriteUrl ="{{ route('save.favourite.company.index') }}" --}}
{{-- let isCompanyAddedToFavourite ="{{ $isCompanyAddedToFavourite }}" --}}
{{-- let reportToCompanyUrl ="{{ route('report.to.company.index') }}" --}}
{{-- let followText ="{{ __('web.company_details.follow') }}" --}}
{{-- let unfollowText ="{{ __('web.company_details.unfollow') }}" --}}
{{--


    } catch (error) {
        console.error('Error in company_details component:', error);
    }
});
// company_details Component JavaScript
// Enhanced with Context7 patterns

--}}
{{-- let addCompanyFavouriteUrl ="{{ route('save.favourite.company.index') }}" --}}
{{-- let isCompanyAddedToFavourite ="{{ $isCompanyAddedToFavourite }}" --}}
{{-- let reportToCompanyUrl ="{{ route('report.to.company.index') }}" --}}
{{-- let followText ="{{ __('web.company_details.follow') }}" --}}
{{-- let unfollowText ="{{ __('web.company_details.unfollow') }}" --}}
{{--


    } catch (error) {
        console.error('Error in company_details component:', error);
    }
});